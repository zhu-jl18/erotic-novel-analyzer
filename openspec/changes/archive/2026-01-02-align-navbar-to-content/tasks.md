## 1. Implementation
- [x] 1.1 Modify navbar structure in `templates/index.html`
  - Remove `px-6 py-4` from outer navbar div
  - Add inner wrapper div with `max-w-4xl mx-auto px-6 py-4 w-full flex items-center`
  - Move existing flex-1 and flex-none divs inside the new wrapper
  - Close the new wrapper div properly

## 2. Verification
- [x] 2.1 Visual check: navbar title and icons align with main content below
- [x] 2.2 Responsive check: alignment holds on different screen widths
